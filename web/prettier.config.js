//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    tabWidth: 4,
    plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
